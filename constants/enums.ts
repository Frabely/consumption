export enum ModalState {
    None,
    AddCarData,
    ChangeCarData,
    DownloadCsv,
    AddFloor,
    ChangeFloorFields,
    AddFloorData,
    DownloadBuildingCsv,
}

export enum Role {
    None,
    User,
    Admin,
}

export enum Page {
    Home,
    BuildingConsumption
}

export enum HomeTabs {
    Statistics = 0,
    ConsumptionData = 1,
}
